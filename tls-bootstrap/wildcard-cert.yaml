apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-312ubuntu-tls
  namespace: ingress-nginx
spec:
  secretName: wildcard-312ubuntu-tls
  issuerRef:
    name: letsencrypt-prod-dns
    kind: ClusterIssuer
  dnsNames:
    - "*.312ubuntu.com"
    - "312ubuntu.com"
