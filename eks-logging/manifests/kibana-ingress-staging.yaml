apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kibana-staging-ingress
  namespace: logging
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    # IP Whitelist - configure for staging access
    nginx.ingress.kubernetes.io/whitelist-source-range: "0.0.0.0/0"
    # Basic authentication
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: kibana-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required - Kibana Staging"
    # Proxy settings for Kibana
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
  labels:
    app: kibana
    environment: staging
spec:
  ingressClassName: nginx
  rules:
    - host: kibana-staging.312ubuntu.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kibana-staging-kibana
                port:
                  number: 5601
