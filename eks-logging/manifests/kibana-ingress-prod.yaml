# Kibana Ingress - Exposes Kibana UI to the internet with security controls
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kibana-prod-ingress
  namespace: logging                         # Must match where Kibana is deployed
  annotations:
    kubernetes.io/ingress.class: nginx       # Use nginx ingress controller
    nginx.ingress.kubernetes.io/ssl-redirect: "true"  # Redirect HTTP to HTTPS (required for prod)
    cert-manager.io/cluster-issuer: letsencrypt-prod   # Auto-provision TLS cert (requires cert-manager)

    # External DNS - auto-creates Route53 DNS record pointing to this ingress LB
    external-dns.alpha.kubernetes.io/hostname: kibana-ubuntu-prod.312ubuntu.com

    # IP Whitelist - Only these IPs can access Kibana (security layer 1)
    # TODO: Replace 0.0.0.0/0 with VPN/office IPs before production deployment
    nginx.ingress.kubernetes.io/whitelist-source-range: "0.0.0.0/0"

    # Basic Auth - Username/password required (security layer 2)
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: kibana-basic-auth  # K8s secret with htpasswd
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required - Kibana Production"

    # Proxy settings - Handle large log queries and slow responses
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"      # Max request size
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"   # 5 min read timeout
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"   # 5 min send timeout
  labels:
    app: kibana
    environment: prod
spec:
  ingressClassName: nginx                    # Required for newer K8s versions
  tls:
    - hosts:
        - kibana-ubuntu-prod.312ubuntu.com
      secretName: kibana-prod-tls            # Auto-created by cert-manager
  rules:
    - host: kibana-ubuntu-prod.312ubuntu.com  # DNS must point to ingress LB
      http:
        paths:
          - path: /                          # All paths go to Kibana
            pathType: Prefix
            backend:
              service:
                name: kibana-prod-kibana     # Service created by Helm chart
                port:
                  number: 5601               # Kibana default port
