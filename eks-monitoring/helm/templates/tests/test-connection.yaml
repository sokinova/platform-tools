apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eks-monitoring.fullname" . }}-test-connection"
  labels:
    {{- include "eks-monitoring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eks-monitoring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
